window.ERSEP_BOLETIN_CONFIG = Object.freeze({
  // Debe coincidir con el nombre del Web Service creado en Render.
  API_BASE_URL: "https://portal-observatorio-ersep-boletin.onrender.com",
  HEALTH_PATH: "/api/health",
  LOGIN_PATH: "/api/login",
  HEALTH_TIMEOUT_MS: 75000,
  RETRY_DELAY_MS: 5000
});
